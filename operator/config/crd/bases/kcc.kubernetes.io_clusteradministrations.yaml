---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
    api-approved.kubernetes.io: "https://github.com/kubernetes/enhancements/pull/1111"
  name: clusteradministrations.kcc.kubernetes.io
spec:
  group: kcc.kubernetes.io
  names:
    kind: ClusterAdministration
    listKind: ClusterAdministrationList
    plural: clusteradministrations
    singular: clusteradministration
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.healthyPods
      name: Healthy
      type: integer
    - jsonPath: .status.unhealthyPods
      name: Unhealthy
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterAdministration is the Schema for the clusteradministrations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterAdministrationSpec defines the desired state of ClusterAdministration
            properties:
              autoHealingEnabled:
                description: AutoHealingEnabled enables automatic healing of failed
                  pods
                type: boolean
              autoScalingEnabled:
                description: AutoScalingEnabled enables automatic scaling based on
                  metrics
                type: boolean
              backupSchedule:
                description: BackupSchedule defines backup configuration
                type: string
              clusterName:
                description: ClusterName is the name of the target cluster
                type: string
              policyEnforcement:
                description: PolicyEnforcement defines security and compliance policies
                properties:
                  allowedRegistries:
                    description: AllowedRegistries defines approved container registries
                    items:
                      type: string
                    type: array
                  enforceNetworkPolicies:
                    description: EnforceNetworkPolicies enables network policy enforcement
                    type: boolean
                  enforcePodSecurityStandards:
                    description: EnforcePodSecurityStandards enables PSS enforcement
                    type: boolean
                  enforceResourceLimits:
                    description: EnforceResourceLimits requires resource limits on
                      all pods
                    type: boolean
                type: object
              resourceQuotas:
                additionalProperties:
                  description: ResourceQuota defines resource limits
                  properties:
                    cpu:
                      type: string
                    memory:
                      type: string
                    pods:
                      format: int32
                      type: integer
                  type: object
                description: ResourceQuotas defines resource limits per namespace
                type: object
            required:
            - clusterName
            type: object
          status:
            description: ClusterAdministrationStatus defines the observed state of
              ClusterAdministration
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              healthyPods:
                description: HealthyPods count
                format: int32
                type: integer
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of last reconciliation
                format: date-time
                type: string
              phase:
                description: Phase represents the current operational phase
                type: string
              policyViolations:
                description: PolicyViolations count
                format: int32
                type: integer
              totalNamespaces:
                description: TotalNamespaces under management
                format: int32
                type: integer
              unhealthyPods:
                description: UnhealthyPods count
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
