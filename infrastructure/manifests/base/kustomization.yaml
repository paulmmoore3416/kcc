apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kcc-system

resources:
  - operator.yaml
  - backend.yaml
  - frontend.yaml
  - ebpf-agent.yaml
  - clickhouse.yaml

commonLabels:
  app.kubernetes.io/name: kcc
  app.kubernetes.io/managed-by: kustomize
